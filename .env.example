# MCP-LSP Bridge HTTP Server Configuration

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Workspace root directory for LSP operations
WORKSPACE_ROOT=/path/to/your/workspace

# Allowed hosts for DNS rebinding protection (comma-separated)
# Example: ALLOWED_HOSTS=qenex.ai,www.qenex.ai,localhost
ALLOWED_HOSTS=qenex.ai

# Logging level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
